#include <stdio.h>
#include <stdlib.h>

int main()
{
    scanf("%d", &test);
    for(i=0; i<test; i++)
    {
        scanf("%d", area);

        while(i*i<area)
        {

            i++;
        }
    }
    return 0;
}
